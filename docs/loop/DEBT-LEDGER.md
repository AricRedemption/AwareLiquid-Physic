# LOOP DEBT-LEDGER — 算力欠账台账(AMM-009;循环元状态账本)

> 欠账=已预注册(判据/seeds/命令齐备)但未执行的实验。协议与判据真相在
> PRD §19 对应锚,本文件只存**状态与指标**,不复制判据。每收尾轮更新。
> 规则(AMM-007/008/009/010/014):open>0 ⇒ 心跳优先清欠、禁新蒸馏族;结案走
> PRD §19 机械验收后置 closed;执行必须过 `./scripts/probe_run` 护栏;
> **Probe-First(AMM-010 硬准入):新 T3 欠账登记前必须随附本机 T1/T2 探针
> 结果(管线校准+时长实测+方向预筛),无探针结果不予登记**。
> **分级(AMM-014)**:档位列 T1/T2 ⇒ **L-debt**(阻塞 DEBT-FIRST);仅 T3 ⇒
> **C-debt**(挂起不阻塞循环,债龄>5 一次性升级;标 C-debt 前须记录探针降级
> 尝试)。当前 2 笔 open 均 L-debt(D-2 余项=TimesFM 权重受限挂起,不阻塞
> 顺序内的 D-4;见清偿顺序 1')。

## 台账

| id | 目标 | PRD §19 锚 | 档位 | 预计 | 状态 | 登记轮 | 债龄(心跳轮) |
|---|---|---|---|---|---|---|---|
| D-1 | UQ 校准检验(重训 probabilistic 500 步,z=(mu−ω_true)/σ,±1.96σ 覆盖∈[90,98]%,seeds 0/1/2) | 轮 84 判读 + **D-1 判读**(已回填) | **T1** | 实测 3×~12s | **closed(判负:覆盖 1.6%/0/0,过度自信方向,N1 上限 L2 坐实)** | 84 | 88 治理轮结案 |
| D-2 | TSFM 零样本参考基线(Chronos/TimesFM on M1 q(t) 外推 k=100,3 seeds,k100 rollout MSE 同表;协议 docs/tsfm-baseline-protocol.md) | 轮 82 判读·欠账登记行 + **D-2 判读**(Chronos 臂已回填) | **T1**(AMM-010 重标,当轮直跑) | Chronos 臂实测 4.6min;TimesFM 臂 ~10min 推理+权重 814MB(下载受限挂起) | **open(收窄:Chronos 臂本机完成;TimesFM 权重下载受限→T3 PR 登记/到位后 T1 补跑)** | 82 | 7(轮 82→89) |
| D-3 | R1 辅助辨识主跑+隐藏卷 998(三臂,判据 MSE 降 ≥15% 且探针 corr ≥0.5;完整命令 docs/pr-d2-r1r2-cloud.md) | 轮 56 欠账登记行 + 隐藏卷 PR §3 + **D-3 判读**(已回填) | **T1**(AMM-010 重标,当轮直跑) | 实测 ~8min | **closed(判负:MSE 比 0.9967 且 corr 0.058,辅助辨识在硬约束架构失效;998 未触发未消耗)** | 56 | 34(轮 56→90)结案 |
| D-4 | R2 视距扫描(k_train∈{8,16,32}×3 seeds,比值单调走阔 ≥1.5x;可拆 3×~20min 逐档跑) | 同 D-3 锚 | **T2** | ~58min(限核墙钟 ~90min,可拆) | open(轮 91:K=8 档完成——逐位复现 e1_final ✓ 管线校验过,geo 比 0.9794 吻合 0.979 预注册基线;K=16 跑中,K=32 待跑) | 56 | 35(轮 56→91) |

注:债龄自登记轮起的心跳轮计数,马拉松停止期不计;D-4 债龄已超报警线
5 轮——历史原因(AMM-008 前无本机档位),闸门 v3(现 v3.1)生效后已在
清偿(D-1/D-3 已结案,D-2 Chronos 臂已清),不升级 BLOCKED-HUMAN;
剩余顺序:**D-4(T2 可拆逐档)优先**,D-2 余项(TimesFM)权重到位后补跑。

## 指标(每收尾轮必算,与 RSI-INDEX B 维度同步)

| 指标 | 定义 | 当前值 | 报警线 |
|---|---|---|---|
| D_count | open 欠账条数(L:2 / C:0,AMM-014 分账) | 2 | 本机档(L)>0 ⇒ 禁蒸馏禁队列迭代(AMM-007/014) |
| D_min | open 预计总分钟(L:~68 / C:0,限核前;D-2 余项 ~10min 待权重,D-4 ~58min) | ~68min | — |
| age_max | 最老 open 债龄(心跳轮) | 34(D-4) | >5 且无本机档位 ⇒ 升级 BLOCKED-HUMAN |
| digest_rate | 已结案(含判负 void)/累计登记 | **2/4 = 0.5**(D-1 判负+D-3 判负,轮 90) | 连续 2 收尾夜 =0 且 D_count>0 ⇒ 优先级最高 |
| IR | 未消化 [行动] 蒸馏条目 / 近 10 轮实验类判读数 | 待首算(≈7/6≈1.2,人工估) | >5 ⇒ 计入 AMM-007 S2 |

## 状态机

open →(probe_run 起跑)→ running →(PRD 判据机械验收过)→ closed
                                        ↘(判负)→ closed(判负如实入档,同样是结案)
协议作废(被新证据取代)→ void(留理由一行)

## 清偿顺序与回写义务

1. 顺序:D-4(T2 后台,可拆 3 档逐档跑);
   (D-1/D-3 已结案;AMM-010 后 D-2/D-3 重标 T1——v1 版本的"T2 后台"
   表述已过时,以本行为准);
   1'. **D-2 进度注记(轮 89,2026-09-19)**:Chronos 臂已完成并判读入档
   (PRD §19 "D-2 判读",实测 4.6min);TimesFM 臂权重下载受限挂起,
   按 GOALS 预授权"环境不可得转 T3 PR"登记,不阻塞后续清偿——
   权重(~/.cache/timesfm/torch_model.ckpt,curl 断点续传中)到位后
   D-2 余项 T1 ~10min 补跑
   (`--models timesfm --timesfm_path <path>`);
2. 每笔结案:PRD §19 写判读(以 `<id> 判读` 为 grep 锚)+ 台账置 closed +
   digest_rate 更新;
3. 全部清零后:恢复 goal_queue 迭代(GRAD-PATH 在队首),蒸馏补池解禁;
4. 隐藏卷纪律不变:seed 998 一次性终跑,结果只做迁移判定。
