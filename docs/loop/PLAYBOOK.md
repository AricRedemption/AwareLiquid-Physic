# LOOP PLAYBOOK — 操作坑与惯例(每轮开场必读,新坑必回写)

> ACE 式演化手册(wave-10 轮 43 落地,2026-09-18)。本文件是循环的**操作记忆**:
> 每轮开场先读;踩到新坑当场回写(一行:坑 → 解法 → 出处轮次);惯例一旦写入
> 即为约束。目标:同一个坑不踩第二次。

## 坑清单(每条都真实付出过代价)

- **`torch.linspace(...).long()` 浮点舍入会越过终点**(轮 22):候选起点
  越过 `S−k_train−1` 导致训练目标索引越界。→ 任何 linspace 取整后必须
  `.clamp(max=合法上界)`;派生量先确认 `steps+1` 语义。
- **`gen_spring` 返回 `steps+1` 个时刻**(轮 22):S 的推导处处可能差一,
  配对比较前先 `S = tensor.shape[1]` 实取,不要用生成参数回推。
- **管道尾命令吞退出码**(轮 22;**轮 92 复发**;**09-20 治理会话三犯**):`pytest -q | tail -1 && 下一步` 中
  pytest 失败也会继续(退出码取自 tail)。→ 门禁链写 `pytest -q && ...`
  或单独跑 pytest 不接管道。(轮 92 复发代价:提交信息误写"全绿"而实际
  1 failed——状态钉死测试被合法清欠过期,fix-forward 修正;门禁一律
  单独跑、看退出码、再提交。09-20 三犯代价:AMM-019 首个提交带 1 个
  失败不变式+失实"全绿"信息已推送,fix-forward 修正——**设计会话同样
  受此坑约束**,门禁链显式退出码:`pytest > /tmp/t.txt 2>&1; EC=$?`。)
- **`no_grad` 杀死 symplectic rollout**(轮 2):哈密顿头 `dV_dq` 内部用
  autograd,eval 前向必须 `torch.enable_grad()` 包裹(参考
  `eval_rollout_mse`),输出再 detach。
- **测试 Namespace 与脚本 argparse 漂移**(轮 13/14/22 三次):给
  `run_one` 加每个新 flag 后,`tests/test_eval_ks_ladder.py` 的 `_args`
  必须同步(已有 keep-in-sync 注释);AttributeError 就是这个。
- **结果目录 `benchmarks/physics_out_v02` 是 gitignored**(轮 2 起):
  结果 JSON 只留本地 + audit,数字必须抄进 PRD 记录,路径仅作本地索引。
- **新产物用子目录隔离**(轮 2 起):绝不覆盖 canonical 产物
  (如第九波 `sample_efficiency_p11.json`);脚本若固定写文件名,用
  `--out_dir 子目录` 隔离。
- **screening 与判定分离**:1 seed 只做闸门/校准,结论必须 3 seeds +
  预注册判据;口径偏离(如用了 k100 而非 k1)必须如实注记(轮 13)。
- **配对基线要看 pool**:同 pool seed 才可逐 seed 配对;`max(sizes)` 不同
  ⇒ pool 不同 ⇒ 只能指示性对比(轮 14 教训)。
- **field_eval 单 seed 全流程 ~7 分钟**:全量拆轮必须预声明(轮 7)。
- **后台长命令**:run_in_background + TaskOutput 轮询;脚本内 `time` 计时
  校准时长(轮 8 起)。
- **策略热更新竞态**(轮 51-52,2026-09-19 夜):用户会在会话运行中途往
  PLAYBOOK/GOAL-PROMPT 提交新规(本夜算力闸门 v1→v2 两次热更);E3 两跑在
  v2 落地后启动,违规(如实入档轮 53,不改写轮 52 判定行)。→ 解法:每次
  心跳启动任何执行类工作前,先 `git log --oneline -3 docs/loop/PLAYBOOK.md
  docs/loop/GOAL-PROMPT.md` 复查规则新鲜度再动手。(出处:轮 51-52;适用
  条件:多会话同仓;验证状态:已验证——本夜三次热更全部命中。)
- **训练循环不传轨迹索引 → 前缀字节指纹查表**(轮 51):需要给模型注入
  每轨迹潜变量(oracle ctx)但 train_semigroup/prefix 只喂 gather 切片时,
  以 t_obs 前缀 `numpy().tobytes()` 为键建行号表,零侵入训练循环;表外
  轨迹回退语义默认值(常数场→零投影即正确 oracle)。(出处:轮 51
  `OracleOperatorWrapper`;适用条件:批 = 母张量精确 gather、轨迹不重复
  生成;验证状态:已验证——A/B 臂与 D2 逐位复现。)
- **1-seed 筛查两连中**(轮 51/52,被验证有效的做法):E1 筛查 ρ 0.730/
  0.719 → 终判 0.726/0.742 同判定;E3 冒烟双探针 ≈0 → 终判同。筛查闸门
  实践有效,保持。(出处:轮 51/52;验证状态:已验证,n=2。)
- **新基准脚本的结果 JSON 顶层必须有 `results` 键**(轮 55):audit_results
  的 schema 硬要求,探针类脚本裸写 summary/per_batch 会被 --check 拒
  (付过 1 cycle)。→ 载荷包一层 `"results": {...}`,args/meta 顶层照旧。
  (出处:轮 55 grad_starvation_probe;验证状态:已验证。)
- **`ctx.retain_grad()` 量激活级梯度**(轮 55):对非叶张量 ctx(推断头
  输出)先 retain_grad 再进 rollout,backward 后 ctx.grad 即 ∂L/∂ctx;
  oracle 对照用 `row.clone().requires_grad_(True)` 叶张量解耦推断图。
  (出处:轮 55 grad_starvation_probe;验证状态:已验证。)

- **经验蒸馏轮最小闭环(轮 61,被验证有效的做法)**:补池时先 grep
  档案既有 query 族(PRD §19 轮 45/60 清单 + scan 档案节标题),本轮
  选**零重叠族**;3 次检索(每族 1 次)即可收口 ≥1 条入库,全轮非算力
  <15 分钟。方向类条目**当轮**追加 GOALS 队尾(写清 done_condition 可
  验证判据),防"档案写了、循环忘了"——队列恢复非空,下轮 goal_check
  自动路由。(出处:轮 61;验证状态:已验证——D5-EXPOSURE 即按此产出。)

- **goal_check 空 check_cmd ⇒ 假阳性 ACHIEVED 误弹**(轮 62,实付 1
  cycle):路由器对队首目标跑 `subprocess.call(check_cmd, shell=True)`,
  入队时漏写 check_cmd ⇒ 空命令退出码 0 ⇒ 刚入队未干活的目标被判达成
  弹出吞掉。→ ① 防线已修:goal_check 空 check_cmd 按未达成路由;
  ② 入队模板必须带轻量验收锚(grep 式 check_cmd,如 `grep -q "锚"
  docs/PRD.md`);③ 误弹后恢复:目标回写队首,在提交信息注明"误弹恢复"。
  (出处:轮 61 入队/轮 62 发现;适用条件:一切经 goal_check 的入队;
  验证状态:已验证——修复后 NOT-Achieved 路由正确。)

- **入新目标前先查工具/产物覆盖面**(轮 63,被验证有效的做法):蒸馏轮
  想入方向类目标时,先 grep 现有 TOOLS 工具与 benchmarks/ 产物目录
  (如 d3_* 已有 tobs8/tobs24 对照),确认新目标是**增量**(连续图谱/
  新框架)而非重复;check_cmd 锚写目标 ID 特异串(如 "D6-INFO-BUDGET
  判读"),不写会过早命中的泛词。(出处:轮 62 假阳性教训的推广;
  验证状态:已验证——D6 入队前查得 fisher_j 闭式已存在,目标即改为
  "图谱+分解"增量定位。)

- **闭式工具的图谱化复用**(轮 64,被验证有效的做法):已有一维/单点
  判据的秒级闭式工具(如 fisher_j)不必重写——沿未扫过的轴(窗口长度)
  做网格扫描 + 与已知渐近律(t³)对照,即可把单点结论升级为图谱级判读
  (无平台判定 + CRB 换算),全轮零算力 <10 分钟。数字抄 PRD,JSON 留
  gitignored 目录。(出处:轮 64;验证状态:已验证——D6 即按此交付。)

- **修复候选的条件性登记法**(轮 65,被验证有效的做法):蒸馏轮发现
  的修复候选若依赖未回传的外部结果(R1/R2 云跑),**不入队**(会与
  等待中的主修复占位冲突、路由器空转)——在 GOALS 队列规则节写
  "X 条件性重入口"注记(触发条件 + 入口内容 + scan 档案指针),
  仿 E2 先例。主结果回传判读时按条件执行,不丢候选。
  (出处:轮 65;验证状态:已验证——R1b 即按此登记。)

- **"积分器可逆"≠"动量翻转回程一致"**(轮 66,机制发现型坑):评估
  依赖文档声明前先做 1 步往返冒烟。velocity-Verlet 映射对称性对任意
  H 成立,但回程一致性 ⇔ T 偶——`_energy_mlp` 学习的 T 天然非偶,
  docstring "exactly time-reversible for ANY T,V" 声明过强(已修订)。
  教训:**结构性质的成立条件要分解**(守恒/可逆各自前提),文档声明
  与实现要 diff。(出处:轮 66;验证状态:已验证——偶 T 对照 1.2e-7
  vs 非偶 T 4.6e-1。)

- **蒸馏条目 [坐标]/[行动] 分流标记**(轮 67,被验证有效的做法):
  经验蒸馏的入库条目强制标注 [坐标](解读用,不进队列)或 [行动]
  (触发目标/协议,进队列或条件性登记)——防"解读坐标"类条目被
  当成待办撑大队列,也防行动点被埋进档案。(出处:轮 67;验证状态:
  已验证——§11 两条 [坐标] 类,队列保持空、停止条件不误触发。)

- **判负后路由表预注册**(轮 68,被验证有效的做法):外部依赖结果
  (云跑/回传)的修复线,除预注册判负标准外,**连同"判负后走哪条路"
  的分流逻辑一起预先写死**(按失败模式路由到独立候选,scan §12.3)——
  回传判读时机械执行,避免在结果情绪下现选方向;且各路线要相互独立
  (处方可叠加),消灭"全判负则无处可去"分支。(出处:轮 68;
  验证状态:已验证——R1/R1b/R1c 三路分流表即按此成文。)

- **自动判定先用已知答案冒烟**(轮 69,坑):把轮 64 手搓判读固化成
  `--window_scan` 工具时,plateau 判定第一版写反(信息涨 200 倍判成
  平台)——第一次冒烟就暴露。→ 手搓轮留存的参照数字(J=142@24 等)
  逐项对上才收工;布尔判定的语义要写在注释里(平台=总增长<2x,与
  增长-vs-t³ 对比无关)。(出处:轮 69;验证状态:已验证——修正后
  plateau=False 与轮 64 判读一致。)

- **同主题跨角度分夜扫描**(轮 70,被验证有效的做法):大主题(如
  liquid 基座)不一次扫完——轮 45 走 system-ID 角度,轮 70 走工程
  前沿角度,角度清单可跨夜接续(PRD §19 轮 45 query 清单存档可查)。
  每轮单族深耕比一晚并扫十族产出的条目更可入库,且天然满足"不同
  query 族"的停止条款证据要求。(出处:轮 70;验证状态:已验证——
  CfC 工程角度产出轮 45 未覆盖的基座坐标。)

- **判读数字必须随附假设声明**(轮 71,被验证有效的做法):派生量
  (CRB、上界、比值)入档时把**假设条件写进同一句**(如"CRB 0.084
  在单位观测噪声下"),否则引用时会丢语义——轮 71 判得本仓数据无噪,
  σ=1 是保守假设而非事实;若当初只记数字,层 1 结论会被反向误读。
  出处轮 71;验证状态:已验证(PRD §19 轮 71 注记即按此成文)。

- **附带诊断字段也要扫**(轮 72,被验证有效的做法):结果 JSON 的
  附带诊断(resolution、energy_drift 等)主判据之外常藏未判读信号
  (轮 72 发现超分容量反向)——蒸馏/等待轮用 10 分钟遍历既有产物
  JSON 的全部键做"再判读",发现的信号按"弱信号如实注记、不下结论"
  入档,并标注复验条件(如"R2 回传时带上")。(出处:轮 72;
  验证状态:已验证——resolution 反向信号即此产出。)

- **入队 check_cmd 用双锚**(轮 73,被验证有效的做法):轮 62 的单
  grep 锚防住了"空命令假阳性",但仍有残余假阳性面——判读锚字先写
  进 PRD、产物却烂尾时,单 grep 锚会误判达成。→ 带产物的目标入队
  用双锚:`grep -q "<判读锚>" docs/PRD.md && test -f <产物文件>`;
  判读锚只在判读写完时存在,产物锚只在文件落地时存在,**两个独立
  事件同时发生才算达成**。(出处:轮 73;适用条件:一切带文件产物
  的目标入队;验证状态:已验证——DH-DESIGN 入队时双锚均未命中,
  goal_check 正确路由 NOT-Achieved→迭代。)

- **kick 括号里的免费符号翻转**(轮 74,坑):给速度-Verlet 的 kick
  加新力项时,力以 **+dt/2·F** 进入(ṗ=−∇qH+F);写成
  `-0.5*dt*(dV + F)` 即静默反号成反阻尼——首跑探针抓到每步 ΔH
  严格为正、随 dt 线性。→ ① 任何新力项**先写单调/守恒探针再接线**
  (判据先行防不了实现错,防的是"接错线无感知");② 探针阈值用
  **对照臂归因**(γ≈0 对照臂 H_end/H0=1.000 vs 阻尼臂 <0.7),
  不拍固定魔数——本轮 1e-4 与 1e-3·H0 两版魔数都被实测打回后重标定。
  (出处:轮 74;适用条件:一切向辛积分器加力/项的改动;验证状态:
  已验证——反号 bug 首跑即捕获,修复后 95 测试全绿。)

- **机制命名四问对账模板**(轮 78,被验证有效的做法):给自有缺口
  找文献命名(如 E1 缺口→谱偏置)时,文档按四问组织——① 机制方向
  与证据方向一致吗(**含适用范围核查**:本轮对账发现 M1 的 ctx=ω
  只是幅值缩放、无频域差异轴,命名当场收窄到 M2 场线——范围收窄
  不是判负,不写范围才是坑);② 排除性证据兼容吗(E3/E4a/D6 逐层
  对账成表);③ 与既有命名分工明确吗(Gradient Starvation=竞争轴
  vs 谱偏置=频序轴);④ 可证伪预言预注册了吗(R1d 逐模式扫描,
  判负=平坦)。四问答不满的命名不配进 N1 正文。(出处:轮 78;
  适用条件:一切"缺口→文献命名"型判读轮;验证状态:已验证——
  四问模板当轮产出范围限定与可证伪预言,防了一次过度声明。)

- **评估口径透镜法**(轮 79 预注册,轮 80 执行收口,已验证):
  把领域标准指标(如混沌文献的 VPT)当**透镜**套到自家既有产物上
  做再判读,而不是重跑实验——三要素:① 先决可行性门(判负①:
  产物数据形状不支持即降级协议登记,本轮实测全库仅 d1b 有三点
  per-k);② 预设诚实出口(判负②:透镜无区分度就记"无区分度",
  本轮 θ=0.1 下两线档位分离但 prefix_n32 k10=0.0983 贴阈,定性
  分离只在 θ∈[0.1,0.16) 窄窗成立——**结论降格为呈现口径,
  不构成新判定**);③ 假设随附(θ 无自然归一化,引用必带)。
  这是轮 72"附带诊断字段也要扫"的推广:再判读透镜不止来自自家
  被忽略字段,也来自领域标准口径。(出处:轮 79/80;适用条件:
  蒸馏轮发现评估口径差时的零算力补齐;验证状态:已验证——
  透镜跑通且两次诚实降格机制都被用到。)

- **跨界基线公平性三声明模板**(轮 82,被验证有效的做法):登记
  类别跨界基线(如零样本 TSFM on 物理状态序列)时,协议文档必须
  含三声明并与数字同表出现——① 数据体制差异(预训练广度 vs 小样本
  结构化);② 非公平基线定位(类别跨界参考,非同台排名);③ 预期
  用途(读者参照:任一结果都有信息量,均不构成排名结论)。缺声明
  的跨界对比会被评审当稻草人打,有了声明则双向免疫(输了不伤、
  赢了不吹)。预期行为只可"预注册,非预言",判定以实测为准。
  (出处:轮 82 tsfm-baseline-protocol;适用条件:一切跨类别基线
  协议;验证状态:已验证——协议一次过判负标准①②。)

- **审计工具必查产物代际**(轮 84,坑+做法):审计某工具的实证语义
  时,必须同时查其产物 JSON 的 `args`(训练步数/规模)——本轮唯一
  probabilistic 产物是 train_steps=5 的语法冒烟(spread=1.2e-4≈0,
  logvar≈先验),若当实跑证据引用会把"模型塌缩到无不确定性"误读
  成实测量。→ ① 引用产物数字前先核 args 代际(冒烟/实跑/隐藏);
  ② 冒烟产物引用必须注明"仅验语法与数据形状";③ 工具有方差语义
  ≠ 有实跑方差数字——两回事。(出处:轮 84;适用条件:一切工具
  语义审计与产物引用;验证状态:已验证——冒烟代际被当场识别,
  N1 表述等级据此限定 L2。)

- **GOALS 入队编辑的孤儿围栏**(轮 83/85 两踩,坑):手工替换
  ` ```yaml\ngoal_queue: [] ` 块时若 old_string 不含闭合围栏,
  新块的闭合 ``` 会与旧围栏叠加成孤儿 ``` 行。goal_check 的非贪婪
  正则不受影响,但文件是脏的。→ ① 入队编辑的 old_string 必须
  包含闭合 ```;② 编辑后 grep -A2 check_cmd 目检一次。(出处:
  轮 83/85;适用条件:一切手工编辑 GOALS 队列 YAML 块;验证状态:
  已验证——两踩两修,第二次当轮发现当轮修。)

- **结构注入必配逃生门登记**(轮 86,被验证有效的做法):架构每
  硬注入一个结构先验,必须同轮登记其"真实结构不符时的逃生门"
  (本轮复核四处:守恒→耗散槽位、T 偶→R1b、可分→Nonseparable
  头、MLP 平滑→如实记录为未解限制)。无逃生门的注入是赌命不是
  先验;逃生门让注入哲学在 N1 答辩中从"教条"变成"可撤销的
  赌注清单"。(出处:轮 86 structure-injection-vs-discovery;
  适用条件:一切架构层结构注入;验证状态:已验证——四处注入
  全部有门,清单一次成文。)

- **检索三连的对照式第三槽**(轮 75,轮 77/81/83/85/87 复验,已验证 n=6,含 1 次查询自修正):
  蒸馏轮 3 检索的槽位分配——① 领域综述(拿标准引用)、② 机制关键词
  直击(拿同题先例)、③ **"X vs Y" 对照式查询**(拿定位素材:混合线/
  竞品线/差异点)。轮 75 第三槽("deep Koopman vs Hamiltonian NN")
  产出 HNK 混合线坐标与 N1 定位三线表;轮 77 第三槽("spectral bias +
  Hamiltonian NN")产出 PINN/算子场景综述与机制实证,直接催生 R1d
  第四路线;轮 81 第三槽("foundation model vs physics-informed neural
  ODE")产出 DYN-layer 盟友引用与天气宏观轴;轮 83 第三槽("deep
  ensembles versus BNN")产出正面对比与模式多样性理论,直接支撑
  "seed 即迷你集成"表述;轮 85 第三槽("hard-coded versus discovered
  inductive bias")产出 hPINNs 硬软对照与过平滑失败模式,支撑分层
  注入哲学表述;轮 87 第三槽("梯度爆炸+辛滚出+长展开")首轮过窄
  被搜索自修正两次后命中——产出前向≠反向稳定坐标与 SymODEN/
  Hamiltonian Matching 谱系。→ 对照槽 query 写法:保留核心对比词,
  修饰词放宽(过窄的复合限定会逼搜索引擎跑题);首轮无产出就换
  分解角度重试(AMM-006)。(出处:轮 75/77/81/83/85/87;适用条件:
  一切定位/写作型蒸馏轮;验证状态:已验证,n=6,1 次修正。)

- **定位文档三轴对照表模板**(轮 76,被验证有效的做法):写"本方法
  vs 邻接线"定位文档时,对照表固定三轴——**线性化在哪 / 结构保证
  在哪 / 情境通道在哪**——每轴一行逼出实质差异,差异讲不满三轴的
  "定位"多半是纯重叠(判负信号);表后必附"何时该换线/借工具"诚实
  边界节,把对手线的真实优势场景写明,防定位文档滑向排位赛。
  (出处:轮 76 koopman-bridge;适用条件:一切 Related Work 定位类
  目标;验证状态:已验证——三轴差异满格 ⇒ 判负未触发,文档一次成稿。)

- **制度性阻塞识别(轮 87 治理轮)**:当循环长期"高产出低判据"(单日 16 族
  蒸馏 vs 零个新实验数据点,RSI 夜 4 掉到 0.45),先问一句——**阻塞是物理性
  还是制度性的?**把全部 open 欠账的预计时长加总,若总量在本机宪法线内
  (本案 ~88min vs ≤1h/任务),则瓶颈是闸门过紧而非算力缺货,解法是分级放行
  (AMM-008)而非继续囤方向库存;欠账要入台账看得见 age 与消化率(AMM-009),
  看不见的债才会被感知成"无限期等待"。(出处:轮 87 治理轮,用户裁定;
  适用条件:一切"队列空+外部阻塞"的马拉松;验证状态:待验证——首次清欠
  回传后核对 RSI B 维度与 K 产出是否回升。)

- **长推理必须逐池流式落盘+断点续跑**(轮 89,2026-09-19):D-2 首跑把
  6 池结果全攒内存、只在结尾写 JSON,工具调用超时杀进程 ⇒ 10 分钟算力
  全损。→ 推理类脚本逐池 append JSONL + 启动时跳过已完成池;probe_run
  无硬超时是给训练的,外层工具调用有自己的超时,别赌。
- **chronos-forecasting 2.x API 变更**(轮 89):`predict(inputs,
  prediction_length=…)` 位置参数(`context=` kw 已删);返回
  `(n, num_samples, horizon)` **样本张量**,点预测要自己 `median(dim=1)`;
  `torch_dtype` 已弃用→`dtype`;chronos-t5 训练视距仅 64,k=100 合法但
  告警(按协议保留并注记)。
- **HF xet 大权重断点续传不可靠**(轮 89):xet 通道 etag 每次请求变化,
  `.incomplete` 换名作废重下,且未认证限速 ~3MB/min(TimesFM 814MB 实测
  ~2.7h)。→ 大权重用 `curl -L -C -` 种子接力(把 .incomplete 拷为目标
  文件再续传),模型侧用本地路径加载(TimesFm `TimesFmCheckpoint(path=)`,
  Chronos `from_pretrained` 本地目录);离线路径参数已在工具预留
  (`tsfm_baseline_eval.py --timesfm_path`)。

- **后台长训练日志 0 字节 ≠ 卡死**(轮 90):field_eval.py 训练 ~8min
  期间 stdout 全缓冲,后台日志一直 0 字节,无法判活(卡死/正常不可分)。
  → 长任务后台跑一律加 `python -u`(或 `PYTHONUNBUFFERED=1`)再接
  probe_run;或脚本内逐 seed print(flush=True)。

- **k_train 档位时长估计要按标度换算**(轮 92):PR 包/协议里的时长是
  **不限速 CPU 口径**,本机 probe_run 限速(4/8 线程+nice)实测要
  ×1.5-2;且 semigroup 训练成本 ≈ 随 k_train 线性(K=8 实测 13min,
  K=16 实测 31min)。→ 同族分档任务登记估计 = **最小档实测 × (k/最大
  档 k 倒数比) × 限速因子 2**,宁松勿紧;用户可见的"超时感"多半来自
  估计定紧而非真慢。

- **`requires_grad` 标志会被 `_grad` 原地改标污染成假真**(轮 93,机制发现型坑):
  hamiltonian.py `_grad` 对数据叶子 `x.requires_grad_(True)` 是**原地**操作——
  同一批 q0/p0 被第一个 dV_dq 改标后,整条滚出链(含 loss)的
  `requires_grad` 在 **eval 态也为 True**(梯度经被改标叶子挂图,但参数收不到
  梯度)。→ ① 一切"是否仍有梯度路径"的语义判据必须落在**梯度流终点**
  (backward 后 `params_with_grad==0`),不能用中间张量的 requires_grad 标志;
  ② eval 态图内存判读要分口径:saved_tensors_hooks pack 钩子在训练态计的是
  驻留至 backward 的量(≈展开图内存),eval 态计的是内层 backward 的**瞬态**
  保存体积(每次调用即释放,实测 ≈ 驻留的 43-44%)——两态数字不可直排。
  (出处:轮 93 grad_path_probe;适用条件:一切涉及 `_grad` 家族头的
  梯度语义审计/内存审计;验证状态:已验证——M1/M2 全档 params_with_grad=0,
  训练态逐组范数正常。)

- **CPU 图内存精确计量用 saved_tensors_hooks,不用 RSS**(轮 93,被验证
  有效的做法):展开式 BPTT 的图驻留内存探针以
  `torch.autograd.graph.saved_tensors_hooks` pack 钩子累计 save-for-backward
  字节——CPU 上 RSS 峰值含解释器/分配器噪声无法分辨图增量;pack 口径
  对 k=8/32/128 给出严格线性标度(斜率 1.0000),共享存储重复保存会使
  绝对值有上界性偏差但标度不受影响(引用绝对值注明"pack 事件总量")。
  (出处:轮 93 grad_path_probe;适用条件:一切 autograd 图内存/图规模
  计量;验证状态:已验证——M1/M2 两头斜率 1.0000,seed 0 逐位复现。)

- **带 ? 题录不得过夜——作者/年份未核即入库**(轮 94,SCAN-AUDIT 抓出
  2 处事实错误):§8.2 "Havrilla et al.?" 实为 PDE-Refiner(Lippe et al.,
  arXiv:2308.05732),§5b CPNO 记 2024 实为 arXiv:2602.01737(2026)——
  链接与题名都对、作者/年份错,单靠"有链接"防不住。→ ① 入库时作者+
  年份当场一次检索核验,AMM-015 才算过关;② 带 ? 的题录**当轮复核**,
  不得带 ? 入库过夜;③ 复核抓出事实错误 ⇒ 当轮修订 + PLAYBOOK 回写
  (AMM-015 ③)。(出处:轮 94 SCAN-AUDIT;适用条件:一切文献条目入库
  与引用;验证状态:已验证——14 次检索复核抓出 2 处事实错误+销 1 处
  轮 88 待办。)

- **降档扫要连【验证状态】行一起扫**(轮 94,坑):轮 88 降档 scan §7 时
  只改了内容行,【验证状态】行的"已验证(检索侧)"漏网继续违反 AMM-015,
  SCAN-AUDIT 全库扫才抓到。→ 缺证性降档修订必须全节过一遍(【内容】/
  【验证状态】/蒸馏结论三处都可能挂着最强断言),否则声明降档了、
  验收状态还挂着旧断言。(出处:轮 94;适用条件:一切 AMM-015 降档
  修订;验证状态:已验证。)

- **聚合/池化层语义先找恒等式,再跑数值探针**(轮 95,机制发现型做法):
  审计 mean-pool 信息损失时,先注意到共享线性层与均值可交换 ⇒ 池化
  输入恒等于均值场编码;再找均值场的动力学不变量——周期网格上
  accel 空间均值逐项抵消恒为零 ⇒ 均值场严格匀速、与介质 c 无关,
  聚合层信息保留是**精确零**(实测 8.6e-11)而非"近似小"。→ ① 一切
  新力项/新聚合/新结构注入,先问"该泛函有没有闭式不变量(求和可
  交换性/守恒量)",一页恒等式顶一万次训练探针;② 机制恒等式能把
  多轮实测异常一次性归因(本例回溯统一解释 E1/E3/D-3 三轮"信息
  没进来");③ 恒等式有适用边界(周期网格/线性共享层),跨模型
  外推前先核前提(M1 无池化不受影响,NBody 无 telescoping 未审计)。
  (出处:轮 95 R1C-AGG 先决探针;适用条件:一切聚合/池化/降采样层
  的信息语义审计;验证状态:已验证——3 单元测试+family 实测。)

- **新机制参数从旧行为起步:零初始化等价锚**(轮 96,被验证有效的做法):
  给既有路径加可学习替代(如 mean→attn 池化)时,把新参数零初始化,
  让第一次前向**严格逐位等于旧行为**(softmax 均匀分 ⇒ 加权和 = 均值),
  再靠梯度自然分化。→ ① 三重红利:回归测试一条 `allclose` 就钉死
  "默认=旧行为"契约、训练起点稳定不突变、消融对照天然干净;② 配套
  测试三件套:旧行为逐位不变式 / 零初始化等价+梯度可达 / 非法值
  构造期拒绝。(出处:轮 96 R1C-AGG model.py pool 参数化;适用条件:
  一切"可学习替代既有固定算子"的改动;验证状态:已验证——
  tests/test_context_pool.py 3 项,冒烟 params 差值即新参数数。)

## 惯例(已固化的流程约束)

- **算力闸门 v3.1(2026-09-19 AMM-008+010,AricRedemption 定策)**:实验动手前,
  预注册必须含**实测校准的预计总时长**(冒烟实测推算,不拍脑袋)。四档定性规则
  (**上限数值以 `scripts/probe_run` 为唯一执行点,本节不复述**)——**T0** 零算力
  不限;**T1** 短任务当轮直跑、当轮判读("能做先做");**T2** 中任务经 probe_run
  护栏后台执行(线程 ⌊0.6×核⌋+nice,同一时刻仅 1 个,次心跳验收);**T3** 长任务
  或全量/多 seed 终局 → **Probe-First 硬准入**(先本机 T1/T2 探针结果:管线校验+
  时长实测+方向预筛,三缺一不登记欠账)→ PR 交付云算力+DEBT-LEDGER 登记。
  **T1/T2 只解锁路由与筛选,终局声明必须 T3 或隐藏卷**;风扇满速/发烫/占用逼近
  红线即人工中止——护机优先于实验;**欠账优先(AMM-007+014)**:本机档
  (L-debt:T1/T2)欠账 >0 ⇒ 禁蒸馏禁队列迭代先清欠;云档(C-debt:T3)挂账
  不阻塞,债龄>5 一次性升级;
  平衡仪表每收尾轮跑 `./scripts/balance_gauge`(AMM-012,数值自动计算不手工维护)。
- 预注册先行:判负标准写进 PRD 再动手;负结果与正结果同等记录。
- 验收门:pytest 全绿 + `audit_results.py --check` 全过才可提交。
- 原子提交:协议+代码+结果+台账+波次记录同一 commit,推 fork。
- 先读 `TOOLS.md` 复用既有工具,再考虑新写;新工具写完回写 TOOLS。
- **单马拉松约定(2026-09-19)**:同一时间只允许一个马拉松会话。任何马拉松
  启动前先看 `docs/loop/GOALS.md` 的 updated——距今 <100 分钟 ⇒ 已有活
  会话,本次启动转为"仅确认状态并结束"。防 CPU 互抢与文件竞态。
- **经验蒸馏入库硬标准(2026-09-19 AMM-015,对评估弱项 1/2 的机制化)**:
  ①**溯源可复核**:【出处】必须精确到可定位题录(arXiv ID/DOI/OpenReview
  forum/会议+年份+作者);域名根/聚合站链接(ResearchGate/EmergentMind/
  alphaxiv 裸链)**不算法标**;题录不确定的自带问号并登记 SCAN-AUDIT 复核;
  年份与性质(oral/keynote/poster)入库时即核验(LieGAN"2023 记成 2024
  keynote"为既成本);②**缺证性声明降档**:一切"空白/极少/无人做"类断言
  只能写"**当前 query 族下未检索到**"——禁止"公认空白/已验证(检索侧)"
  式最强断言(不存在性命题的最弱证据配最强断言=自欺通道,§7 既成本)。
- **经验蒸馏轮(QUEUE-EMPTY 时执行,2026-09-19 制度化)**:社区/论文扫描
  找有效经验,每条入库必须含【出处链接+适用条件+验证状态(待验证/已验证/
  不适用)】并分流:操作类→PLAYBOOK 本节;工具类→实现后进 TOOLS;方向类
  →GOALS 队尾。本轮交付=≥1 条入库条目或 ≥1 个新目标,否则不算完成——
  防止变成无限读论文。社区说有效 ≠ 对我们有效,有效性由后续循环与
  RSI-INDEX 的 K/E 变化收口。
- 对循环自身程序的改进 = 提案制,写 `AMENDMENTS.md`,未经用户批准不得
  自行更改 cron 提示词或本 playbook 的"惯例"节(坑清单可直接追加)。
- **简化等效铁律(2026-09-20 AMM-021,Arredemption 定为迭代原则)**:任何
  简化/重写/清理,**前后产出与行为必须等效**。执行法:①等效清点单先行
  (列出每条行为及其存续位置,删重复不删唯一源);②机械层双跑比对
  (改动前后各跑一次路由器/门禁,输出逐字节一致);③提交信息附清点
  结果。区分两类问题:**成本型**(大文件被读=多花 token,不产生错误——
  容忍,不为省钱加间接层)vs **复杂型**(规则/指针网络/双写=弱 agent
  读错,产生错误——才清理);Context Rot 研究(Chroma 2025)证实施行
  指令本身也是干扰物,弱 agent 在干扰物密度下退化最剧。既成本:GOALS
  条件重入口 19 行→4 行,双跑逐字节一致(09-20)。
